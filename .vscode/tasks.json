{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "build Debug",
      "type": "shell",
      "command": "cmake --preset Debug; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; cmake --build --preset Debug",
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "problemMatcher": []
    }
  ]
}
